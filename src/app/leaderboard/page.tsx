"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { api, RequestError } from "@/lib/api-client";
type Entry = { userId: string; name: string; points: number; games: number; wins: number };
export default function LeaderboardPage() { const [rows,setRows]=useState<Entry[]>([]); const [error,setError]=useState(""); const [loading,setLoading]=useState(true); useEffect(()=>{ void api<Entry[]>("/api/leaderboard").then(setRows).catch(e=>setError(e instanceof RequestError?e.message:"Classifica non disponibile.")).finally(()=>setLoading(false)); },[]); return <main className="standalone-page"><Link className="back-link" href="/">← Torna al tavolo</Link><section className="panel leaderboard-page"><span className="eyebrow">BRISCORE</span><h1>Classifica<span className="lime">.</span></h1><p className="muted">Solo sessioni concluse ufficialmente.</p>{loading&&<p className="muted" role="status">Caricamento classifica…</p>}{error&&<p className="info" role="status">{error}</p>}{!loading&&!error&&!rows.length&&<p className="muted">La classifica si riempirà dopo la prima sessione conclusa.</p>}<div className="leaderboard-list">{rows.map((r,i)=><div className={i===0?"leaderboard-row winner":"leaderboard-row"} key={r.userId}><span><b>{i+1}.</b> {r.name}<small>{r.games} {r.games===1?"sessione":"sessioni"} · {r.wins} {r.wins===1?"vittoria":"vittorie"}</small></span><strong className={r.points>=0?"positive":"negative"}>{r.points>0?"+":""}{r.points}</strong></div>)}</div></section></main>; }
